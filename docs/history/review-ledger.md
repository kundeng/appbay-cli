# Review ledger

The durable record of the review track described in `CLAUDE.md`. Chat is where a note is
discussed; this file is where its outcome lands. One row per finding, appended in the turn
it is raised, updated in the turn its status changes. The position line at the top is what
the next session resumes from.

**Position (2026-09-06):** S48 in flight (review rounds; core and CLI regions at LOW-only as of round 9, the round-9 diff pending) · open: 44 · next after S48: Kun's decisions D1, D2, D3 (review 04) and row 28; then issue #11 (ship the injector); row 22 (single-node Swarm); issue #12 (a trait owns its converge); S49 (config loader, eslint, builds.ts spawns). The human review track starts at docs/steering/product.md.

## Protocol

- A finding gets a row when it is first stated, whether by Kun or by Claude, with the
  file and line it was read from and the shape it matches (1 reported-unobserved success,
  2 same behaviour twice, 3 test that runs nothing, S structural).
- `verified` means the claim was read from the named lines or produced by a command that
  is quoted. `reasoned` means it follows from reading but was not run. A reasoned row that
  matters gets run before it becomes `confirmed`.
- Status moves `open` → `confirmed` or `rejected` → `fix proposed` → `committed <sha>`.
  A fix is proposed only after Kun calls the row a defect, and lands only after Kun says so.
- No code changes while the review is on a file, beyond the one-file diff for a confirmed row.

## Findings

| # | file:line | shape | claim | evidence | status |
|---|---|---|---|---|---|
| 1 | `packages/core/src/services/deploy-service.ts:820-830` | 1 | `deploy()` treats a `null` from `findCrashedServices()` as "nothing crashed"; the contract at line 197 says `null` means "could not inspect". | verified, read | committed c643886 (S36 2.1) |
| 2 | `deploy-service.ts:105` | 1 | `composePs()` retries without `-a` on any non-zero exit. On Docker, `-a` is what lists stopped containers (`docker compose ps --help`, v5.1.2), so a transient failure of the first form makes the crash detector vacuous on Docker too. | verified, help text run locally | gone with S41: no compose ps parse; the API lists all containers |
| 3 | `deploy-service.ts:817-850` vs `930-975` | 2 | The converge block (up, crash check, Caddy install, count) exists twice. Only the second snapshots before and after; the first reports `deployed` without observing a container. | verified, read | narrowed: both paths share resolveDeployEnv, installRoute, the crash check and the observer; the converge snapshot is on the unchanged path only, by design (the new path just created the container) |
| 4 | `deploy-service.ts:205` | 1 | Crash test is `state === "exited" && exitCode !== 0`. A `restart: always` service in a crash loop reports `restarting` and is counted as running. | reasoned | committed (S42): restarting counts as restart-looping |
| 5 | `deploy-service.ts:819` | 1 | The crash check runs once, immediately after `up -d` returns. A container that fails a few seconds in is reported deployed. | reasoned | committed (S42): a second read after a 3 s grace when the first is clean |
| 6 | `apps/cli/src/commands/ps.ts:64-110` | 2 | A second parser of `compose ps --format json`. No banner skip and reads `Name`, not `Names[]`, so `appbay ps` lists nothing on a Podman host. The fix in d56474f reached `deploy-service.ts` only. | reasoned from both sources; no Podman host here | committed 0047ed1 (S37 2.2): one parser in runtime/observe.ts |
| 7 | `deploy-service.ts:100-270` | S | The container-state readers (`composePs`, `parseComposePsJson`, `findCrashedServices`, `snapshotContainers`) have no deploy input and belong in `packages/core/src/runtime/`, which already claims `ps --format` at `container-runtime.ts:186`. `didConverge` can go either way. | verified, read | committed 0047ed1 (S37 2.2/2.3): runtime/observe.ts |
| 8 | repo-wide | S | State is observed by parsing CLI output. The Engine API on the socket the CLI already resolves (`apps/cli/src/commands/server.ts:70-79`) returns typed `State`/`Status`; verified locally with `curl --unix-socket /var/run/docker.sock 'http://localhost/containers/json?all=1'`. Mutation stays with `compose up -d`, observation should move to the socket. The Go rewrite already draws that line (`stackbay/docs/design/lessons-paid-for.md:222-229`). Podman's compat API claim is from memory. | verified for Docker, reasoned for Podman | committed (S41): runtime/engine-api.ts + observe.ts; no CLI parse for container state; Podman on issue #9 |
| 9 | `deploy-service.ts:616`, `apps/cli/src/commands/up.ts:25-31` | S | RFC-001 §4 says a namespace absent from the manifest is "decided at deploy time". `compile()` accepts one (`compile.ts:104`, resolved at `:422`) but `deploy()` never passes it and no command has a `--namespace` flag, so a namespace is a manifest field only. The compose project name is the app directory (measured: `docker compose config` on `renders/whoami/` → `whoami`); the namespace never reaches it, by design via `install --as`. `instance.ts:92` calling `project` "the compose project prefix" is stale. | verified, read and run | resolved by S40's shape: a namespace is a manifest field with a values file; one render per app dir cannot host two namespaces, so a deploy-time flag is not the mechanism |
| 10 | `packages/core/src/compiler/scope-resolver.ts:50`, `compile.ts:462-466` | S | The goal RFC-001 §4 was opened for (F49: two instances of one app in one home) is met for identity and not for values. `VALID_SCOPES` is still `service, environment, project`; `environment` is still fed `{}` per run; no `app` scope exists, so `host: litellm.${{project.DOMAIN}}` in two `install --as` copies renders the same vhost. F49's second fix (`${{app.NAME}}`) was never tasked; RFC 4.6 (per-namespace values) was decided against in S32. The namespace never touches the hostname. | verified, read | committed 4f69684 (S40): default host from identity, app scope, duplicate-host compile error |
| 11 | `deploy-service.ts:104,194,223,256` vs `:408` | 2 | Four functions return `\| null` meaning "could not inspect", each with a comment saying null is not a verdict. `runCaddyCommand` in the same file, same commit (d56474f), returns a status union. Row 1 is the consequence: `string \| null` cannot distinguish "nothing crashed" from "could not ask", so `if (crashed)` at :820 collapses them, and the test at `deploy-converge.test.ts:242` pins the collapse. | verified, read | fix proposed: `Inspection<T> = {kind:"ok"; value} \| committed c643886 (S36 2.1): `Inspection<T>` on the four; both paths handle unknown |
| 12 | `deploy-service.ts:190-197`, `deploy-converge.test.ts:205-212` | 3 | Both name `apps/web`'s deploy worker as the second caller the export exists for. No `apps/web` in this tree; `git log -- apps/web` is empty. The contract is enforced only by a test in this repo that asserts the null ambiguity. | verified, ls and git log | committed 127fc05 (S48): the retired type is gone, the two strings reworded |
| 13 | `deploy-service.ts:416`, `edge-identity-service.ts:139,157`, `setup.ts:303,611` | 1+2 | The caddy system app compiles to `container_name: appbay.system.caddy.caddy` (run in a scratch home). The deploy path execs into `appbay.caddy.caddy` / `appbay.caddy`, finds neither, returns `unavailable`, and every ingress app is counted `startedButUnrouted` + `failed` on any host whose edge was recreated after 0ad179a (2026-08-31). The migration path was repointed to the label (9da6a61); the deploy path was not. No deploy journey has run since. | verified, compile run; proved live on the local Docker | committed 73660f7 (S36 2.2) |
| 14 | `apps/cli/src/commands/apply.ts:111-116` | 1+2 | Second deploy path: `up -d` exit 0 prints `deployed`; no crash check, no Caddy install. | verified, read | committed 8952342 (S36 2.6) |
| 15 | `apps/cli/src/commands/update.ts:195-200` | 1 | try/catch around `spawnSync`, which does not throw on non-zero (proved); `done` prints on every failed pull. | verified, run | committed 53c783a (S36 2.8) |
| 16 | six CLI commands | 2 | 12 invocations spawn the literal `"docker"`, bypassing `containerBin()`; `dive.ts:37` hardcodes the socket path. | verified, grep | committed 5381076 (S36 2.7) |
| 17 | `health/checks.ts:262-275` and six more (sweep) | 1 | `passed: true` when the check could not look; `doctor --json` `ok` derives from Failed only. | verified, read | committed d656581 (S36 2.4) |
| 18 | `edge-migration-service.ts:77-81` | 1 | `ps` non-zero → `lines = []` → every edge port reported free. | verified, read | committed 6bfb832 (S36 2.5) |
| 19 | see seam-review.md Seam 4 | 2 | `tryExec` ×3, `findOllamaContainer` ×3, `.env` parser ×3, home resolver ×5, `compile()` caller ×5 identical, second doctor in `setup.ts:284`, `appbay.yaml` parsed without schema ×4. | sweep, spot-checked | committed 84add49…f8c00f7 (S37): tryExec ×1, home resolver ×1, instance loader ×1, compileInstall ×1, semver ×1; setup status keeps its own rows |
| 20 | see seam-review.md Seam 5 | 3 | Four tests assert only `toHaveBeenCalled*`; no test compiles a system app and asks the deploy path which container it execs into, which is how row 13 slipped. | sweep, spot-checked | committed 73660f7 (S36 1.2): the compile-then-target test exists; the mock-call tests are S38 |
| 21 | `docs/steering/product.md` | S | Owner decision, 2026-09-05: a namespace is identity and a value store, `${{namespace.KEY}}` from a per-namespace values file over the per-host `${{project.KEY}}`. Reverses S32's rejection of RFC-001 4.6 and resolves R4 in the loader direction; the derived default host (row 10) becomes the default value inside that store. | decided by Kun | committed 4f69684 (S40): etc/namespaces/<ns>.yaml → ${{namespace.KEY}}; corrected by S43: `${{ns:KEY}}` is the one scope, project is the unit of composition |
| 22 | `docs/steering/product.md` | S | Open question from Kun: single-node Swarm mode for its runtime secret store. Unmeasured on both runtimes. | open question | open |
| 23 | secrets trait, five injection modes | S | Kun: it is not established that every mode keeps the secret out of a render, argv, and log. The security-review pass (S38) audits each mode; product.md states intent, not guarantee, until then. | raised by Kun | audited in S38: row 25 found and fixed; the secrets guide now states what each mode exposes; Podman rootless behaviour per mode still unaudited |
| 24 | `compiler/identity.ts: shepherdTarget`, `shepherd/run-shepherd.ts:69` | 1 | A shepherd `share` option runs `--network=container:appbay.<app>`, and no container has that name (they are `appbay.<app>.<svc>`). No trait sets `share` today, so it is dead; the day one does it fails at the runtime. Centralised in identity with the caveat. | verified, read | committed (S42): the share target is the app's container found by label |
| 25 | `secrets/resolve-for-deploy.ts` (both wrapper writers), `shepherd/run-shepherd.ts` | 3 (security) | Both wrapper injection modes passed the secret bytes inside `sh -c` on the docker argv on the HOST: `wrapper-file` the plaintext values, `entrypoint-wrapper` the encrypted bundle and the seed that decrypts it, side by side. Readable from /proc by any host process for the life of the run. Found by the S38 modes audit Kun asked for (row 23). | verified, read | fixed in S38: payload on stdin (`-i`), names validated; test pins argv clean |
| 26 | `docs/steering/product.md`, `compiler/scope-resolver.ts` | S | Kun, 2026-09-05: S40's four scopes were too many. System is physical (this installation), project is intent (what runs together), namespace is one deployment and the only value scope above a service; collections and tags are labels; `<scope>:` not dot; `when:` is about the project. | decided by Kun | committed (S43): `${{ns:KEY}}`, `project:` manifest field, `etc/projects.yaml` |
| 27 | `compiler/compile.ts` Stage 2a, `traits/definitions/ingress.ts: attachToSharedNetwork` | 2 | Identity (container name, `com.appbay.*` labels, shared alias) is produced only inside `transformUpstream`, which runs only with `upstream:`; a plain app deploys as `echo-echo-1` with no labels, invisible to every by-label observer. Found by the S43 live proof. The ingress alias was a second hand-built copy (`<app>_<svc>`) that disagreed with the fragment for a namespaced app; that half is fixed. | verified, run | alias fixed in S43; identity for every app committed (S44), issue #10 closed |
| 28 | `traits/definitions/ingress.ts:80-105`, `schemas/instance.ts: domain, acme_dns_provider` | S | Kun, 2026-09-06: how to handle a multi-homed host (one box reachable as `home.lan`, a tailnet name, a public name). Proposal: keep one `DOMAIN` per namespace and one host per route; put the multi-homing in DNS, not in the edge. An owned domain with a DNS-01 wildcard (already `acme_dns_provider`) whose records point at the address each network sees, or a DNS system app answering the wildcard per interface. What appbay adds is observation: doctor resolves `<stem>.<domain>` and checks it is one of this box's addresses. Fanning routes out over a system-level domain list is the fallback, and costs a per-domain TLS policy and a set-valued host collision check. | proposed, not decided | open |
| 29 | `services/edge-migration-service.ts: inspectEdgePorts` | 2 (L10) | Parsed `ps --format {{.Labels}}` text; Docker renders `k=v,…`, Podman `map[k:v …]`, so on Podman the outgoing edge was never recognised and every `edge migrate` refused, blaming the edge it was replacing. The last CLI parse S41 left. | verified, run (Podman 5.8) | committed (S45): over the Engine API; tests on a unix-socket server; arch allowlists shrink by two |
| 30 | `apps/cli/src/commands/edge.ts:108` | 1 | `from` was "the other provider", not the one serving: after `init --ingress-provider` the config named an edge not yet running and `migrate` reported "<other> is still serving" whichever was asked. | verified, run | committed (S45): the outgoing edge is the one observed running by label; migrating to the serving one is refused by name |
| 31 | `system-apps/traefik/config/dynamic/tls-options.yml` | 1 | `sniStrict: true` on the default TLS options with no certificate for a local TLD: every HTTPS handshake to `*.appbay.local`, the seeded default, failed "unrecognized name", and there is no HTTP router for local domains. The default Traefik install served nothing. | verified, run (Docker, Podman) | committed (S45): strict SNI dropped from `default`, kept in `modern`; 200 on both runtimes |
| 32 | `secrets/resolve-for-deploy.ts: STDIN_FILE_WRITER` | 1 (security path) | `IFS== read` treated base64 padding as delimiters, so any secret whose length is not a multiple of three failed "base64: truncated input" in both wrapper modes, on every runtime, since S38. The S38 test pinned a padded payload the writer could not read (shape 3). | verified, run | committed (S45): space-separated payload; the test runs the writer on padded values |
| 33 | `traits/definitions/secrets.ts:249`, `tools/appbay-inject` | 1 | `entrypoint-wrapper` mounts `$APPBAY_HOME/bin/appbay-inject`, which nothing builds or installs; Docker binds a directory in its place, Podman refuses the mount. With a hand-built binary the mode works on both runtimes. | verified, run | issue #11; the guide states the manual prerequisite |
| 34 | `traits/definitions/secrets.ts:249` | 1 | The injector bind mount had no SELinux label; on an enforcing host the container could not read it and exited 139 before the entrypoint ran. | verified, run (Fedora 44, enforcing) | committed (S45): `:ro,z`; pinned by a compile test |
| 35 | `apps/cli/src/commands/init-system.ts:260,500`, `runtime/container-runtime.ts:261` | 1 | The docker-group grant keyed on a probe for `docker` taken before the install step, so a fresh host never granted the service account the socket; and `systemdUnit: "docker"` is not a unit name, so the ordering was dropped. The unit test fed `docker.service` itself and passed (shape 3). | verified, run (Rocky 9.8) | committed (S45): post-bootstrap state decides; `docker.service`; a plan test with no runtime present |
| 36 | `docs/history/2026-09-06-review/` | R | The post-fix full review: 21 findings, seven fixed at once (S46: phantom `wrapper-live` mode, five deploy failure branches that did not block dependents, a shallow overlay merge, one-shot services never ready, `--as` installs sharing vault keys, `server status` folding unknown, a shell string in `up --open`). Three need Kun: D1 plaintext `.env.local` fallback when the vault is locked, D2 the master password as a file beside the vault, D3 the backup trait on a CLI-only install. | verified, read | S46 committed; D1–D3 open |
| 37 | `deploy-service.ts:858`, and 15 sites | 1 | The counters are kept live during the loop (15 `failed++`, 15 `notReady.add`) and the readiness wait decrements one it had already granted: the verdict is written before the evidence is in. | verified, counted | committed e47cf9a (S47): the report is a fold in deploy/report.ts |
| 38 | `deploy-service.ts:610-734` vs `735-841` | 2 | The converge is written twice from the env step down. The new/changed path never takes the snapshot pair, so `convergeAction` is unrecorded there except when the crash check is unknown; row 3's "by design" narrowed the finding without removing the second path. | verified, read | committed e47cf9a (S47): one chain per app in deploy/converges.ts; the project link snapshots on every plan status |
| 39 | `deploy-service.ts:647-663` vs `traits/definitions/secrets.ts:316` | 2 | Wrapper-file secrets are materialised twice on the new/changed path: an explicit `resolveWrapperFileSecrets` call, then the pre-deploy action the secrets trait emits for the same references. Two shepherd runs, two error formats. | verified, read | committed e47cf9a (S47): the explicit call is gone; the trait's pre-deploy action is the one writer |
| 40 | `apps/cli/src/commands/restart.ts:27-50` | 2 | A second `writeRenderedOutput`. It writes auxiliary files under `renders/<app>/` where core writes them under `APPBAY_HOME`, so `appbay restart` puts an edge fragment where no edge reads it, and skips the Caddy transaction entirely. | verified, read | committed 127fc05 (S48): restart stops through stopApps and starts through deploy(); the copy is gone |
| 41 | `deploy-service.ts:443, 776` | S | `stateDir` is computed and never read. The `existsSync` guard on the unchanged path is dead: an unchanged plan means the render existed at compile time (`compile.ts:748-756`). | verified, read | committed e47cf9a (S47) |
| 42 | `scripts/journeys/s26-journey-apply-success.sh:36` and 15 more | 3 | Sixteen journeys `cd /home/ubuntu`, the multipass user's home, so they cannot run on the Lima guests (`/root`, `/var/lib/appbay`) whatever `HOME_DIR` says; only `s29` ran for S47. A journey the fleet cannot run is a test that runs nothing on the second runtime. | verified, run (podman guest: `cd: /home/ubuntu: No such file`) | committed 127fc05 (S48): WORKDIR in eleven journeys and the README; runs on both guests under S48 3.1 |
| 43 | `apps/cli/src/commands/setup.ts:124,143,196` | Q3 | `scaffoldTraefikConfig` writes three config files through `bash -c "cat > <path> << 'EOF'\n<yaml>EOF"` with the path and the content interpolated into the shell string; a home path containing a double quote breaks the command, a YAML line reading `EOF` truncates the file, and `mkdir`, `touch`, `chmod` are spawned where `node:fs` does the same. Live: called from `setup` at :525. | verified, read | committed (S48 round 1): node:fs writes; the arch rule now refuses any spawn of the container binary outside runtime/ |
| 44 | `compiler/identity.ts` (labels), `runtime/observe.ts:126` | S | Two appbay homes on one host share compose project names: a container carries `com.appbay.app` and `com.appbay.namespace` but nothing that names the install it came from, so an observation or a `down` by project from one home reaches the other's containers. Measured 2026-09-06 on the Podman guest: a render-less `down` from a scratch home stopped the real install's `whoami` and `traefik` (restored). S48 makes the render-less `down` refuse; an install label on every container is the fix, and it is a compile-time identity change. | verified, run | open: S49 or its own sprint; Kun decides the label's name |
