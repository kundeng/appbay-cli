record: probe-70-four-passwords-at-head-and-which-are-collapsible
type: probe
title: four-passwords-at-head-and-which-are-collapsible
timestamp: "2026-08-30T22:30:34-04:00"
claim: appbay-cli asks an operator to hold four distinct passwords, and they gate four different things
verdict: 'supports'
key_evidence: 'four distinct passwords exist at HEAD and gate four different things: control-plane (minted admin.ts:33, hashed into etc/control-plane/users.yaml), edge user (edge-identity-service.ts:87, hashed into etc/apps/caddy/config/security/users.json), vault master (plaintext at etc/vault-password, six read sites), and KeePass master (etc/kdbx-password). Only the KeePass one is collapsible today: keepass.ts resolveDbPassword falls through to the vault master at steps 3 and 4, so it is separate only when etc/kdbx-password exists. The control-plane and edge passwords are NOT the same thing — the first signs in to appbay, the second signs a human in to the deployed apps through Caddy'
exit_code: 0
duration_s: 1
command: bash investigations/2026-08-30-preprod02-sim-stack-review/src/preprod02-sim-stack-review/password_census_at_head.sh
code:
  - path: investigations/2026-08-30-preprod02-sim-stack-review/src/preprod02-sim-stack-review/password_census_at_head.sh
    sha256: 66c32c05fc8ba9be89b4d3d834864a25fb777cee80650ec4e4d69fd3e583f0f0
    stored: raw/code/66c32c05-password_census_at_head.sh
output_kind: text
output:
  text: |
    == repo under test ==
      /Users/kundeng/Projects/appbay-cli  bd32116  v0.0.1-alpha.11
      (the sim runs v0.0.1-alpha.6; these line numbers are HEAD)

    == 1. CONTROL-PLANE password — signing in to appbay itself ==
      admin.ts:33:    const password = options.generate ? randomBytes(24).toString("base64url") : await readPasswordFromStdin();
      admin.ts:66:      const passwordHash = hashControlPlanePassword(password);
      admin.ts:19:  .description("Reset the password for signing in to APPBAY ITSELF (control-plane account, etc/control-plane/users.yaml). Not an edge use
      stored: "control-plane",18-"users.yaml",
      control-plane-user-service.ts:15:export const CONTROL_PLANE_USERS_RELATIVE_PATH = join(
      control-plane-user-service.ts:18:  "users.yaml",
      control-plane-user-service.ts:36:    this.path = join(appbayHome, CONTROL_PLANE_USERS_RELATIVE_PATH);

    == 2. EDGE USER password — a human signing in to the DEPLOYED apps via Caddy ==
      edge-identity-service.ts:9:export const EDGE_USERS_RELATIVE_PATH = join("etc", "apps", "caddy", "config", "security", "users.json");
      edge-identity-service.ts:87:  async create(input: { username: string; email: string; password: string; roles?: string[] }): Promise<EdgeUser> {

    == 3. VAULT MASTER — decrypts vault.enc ==
    services/vault-service.ts:102:  const passwordFilePath = join(appbayHome, "etc", "vault-password");
    services/vault-service.ts:149:  const vaultPasswordPath = join(appbayHome, "etc", "vault-password");
    services/vault-service.ts:187:  const passwordFilePath = join(appbayHome, "etc", "vault-password");
    services/vault-service.ts:233:  const passwordPath = join(appbayHome, "etc", "vault-password");
    services/vault-service.ts:292:  const passwordPath = join(appbayHome, "etc", "vault-password");
    secrets/providers/vault.ts:317:  const passwordFilePath = join(appbayHome, "etc", "vault-password");
      vault-service.ts:95: * Priority: APPBAY_VAULT_PASSWORD env var > $APPBAY_HOME/etc/vault-password file.
      vault-service.ts:99:  const envPassword = process.env.APPBAY_VAULT_PASSWORD;

    == 4. KEEPASS MASTER — opens the .kdbx, and its four-step fallback ==
      keepass.ts resolveDbPassword 2:  // 1. Dedicated KeePass env var
      keepass.ts resolveDbPassword 7:  // 2. Dedicated KeePass password file ($APPBAY_HOME/etc/kdbx-password)
      keepass.ts resolveDbPassword 19:  // 3. Shared vault password env var (fallback)
      keepass.ts resolveDbPassword 24:  // 4. Shared vault password file (fallback)
      keepass.ts resolveDbPassword 33:  return "";
      ⇒ steps 3 and 4 fall back to the VAULT master, so #4 is optional today,
        and is a separate password only when etc/kdbx-password exists.

    == which of the four the sim actually holds ==
      (probe-66: APPBAY_HOME had vault.enc, state/generated-values.yaml, state/operator-id)
      etc/vault-password is the only password FILE appbay writes on init:
      secrets.ts:apps/cli/src/commands/secrets.ts:270:      console.log(`Vault password file repaired: ${result.passwordPath}`);
      secrets.ts:apps/cli/src/commands/secrets.ts:352:    console.log(`Password stored at ${result.passwordPath}`);
      secrets.ts:apps/cli/src/commands/secrets.ts:579:      console.log(`Password stored at ${result.passwordPath}`);
